version: '3.8'

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: sqlite+aiosqlite:////data/claude_bot.db
    volumes:
      - bot_data:/data

volumes:
  bot_data:
